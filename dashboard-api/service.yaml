apiVersion: v1
kind: Service
metadata:
  name: dashboard-api
  namespace: webui
spec:
  selector:
    app: dashboard-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
